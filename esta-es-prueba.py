def main():     # Crear un diccionario para almacenar las conexiones entre ciudades     red_ferrocarriles = {}      # Leer el número de ciudades     N = int(input("Número de ciudades: "))      # Leer los nombres de las ciudades y sus conexiones     for i in range(N):         nombre_ciudad = input(f"Nombre de la ciudad {i + 1}: ")         conexiones = input(f"Ciudades enlazadas (separadas por comas) para {nombre_ciudad}: ").split(',')         red_ferrocarriles[nombre_ciudad] = conexiones      # Pedir al usuario las ciudades de origen y destino     ciudad_origen = input("Ciudad de origen: ")     ciudad_destino = input("Ciudad de destino: ")      # Verificar si hay una vía directa entre las ciudades     if hay_via_directa(red_ferrocarriles, ciudad_origen, ciudad_destino):         print("Hay una vía directa entre las ciudades.")     else:         print("No hay una vía directa entre las ciudades.")  def hay_via_directa(red_ferrocarriles, ciudad_origen, ciudad_destino):     visitados = set(
